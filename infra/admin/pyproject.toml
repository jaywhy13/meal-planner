[project]
name = "admin"
version = "0.1.0"
description = "On-demand maintenance instance for production Django shell access"
requires-python = ">=3.10"
dependencies = [
    "pulumi>=3.230.0",
    "pulumi-aws>=7.25.0",
]
