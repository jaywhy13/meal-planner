[project]
name = "prod-shell"
version = "0.1.0"
description = "Interactive shell instance infrastructure for the meal planner"
requires-python = ">=3.10"
dependencies = [
    "pulumi>=3.230.0",
    "pulumi-aws>=7.25.0",
]
